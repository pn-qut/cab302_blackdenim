<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.habittracker.controller.MainShellController">

    <left>
        <VBox spacing="20"
              prefWidth="260"
              styleClass="navigation">

            <Button text="Home"
                    onAction="#HomeBar"
                    maxWidth="Infinity"/>

            <Button text="Habits"
                    onAction="#HabitsBar"
                    maxWidth="Infinity"/>

            <Button text="Community Goals"
                    onAction="#CommunityGoalsBar"
                    maxWidth="Infinity"/>

            <Button text="Leaderboard"
                    onAction="#LeaderboardBar"
                    maxWidth="Infinity"/>

            <Button text="Achievements"
                    onAction="#AchievementsBar"
                    maxWidth="Infinity"/>

            <VBox VBox.vgrow="ALWAYS"/>

            <Button text="Profile"
                    onAction="#ProfileBar"
                    maxWidth="Infinity"/>

            <Button text="Settings"
                    onAction="#SettingsBar"
                    maxWidth="Infinity"/>

        </VBox>
    </left>

    <center>
        <StackPane fx:id="contentArea"/>
    </center>

</BorderPane>
